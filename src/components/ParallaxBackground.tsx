import { useEffect, useState } from "react";

const ParallaxBackground = () => {
  return (
    <>
      {/* The dot-grid background is now handled by body CSS in index.css */}
      {/* This component is kept for compatibility but no longer renders visible elements */}
    </>
  );
};

export default ParallaxBackground;
